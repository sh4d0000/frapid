#!/usr/bin/env groovy 

def cli = new CliBuilder(usage: 'frapid-deploy')
def opt = cli.parse(args)

if ( opt.h ) {
   cli.usage()
   return
}


println("Deploying project...")
def frapid = new Frapid()
frapid.deploy()
println("Project deployed")
